# تقرير مقارنة النماذج - W3D4

## 1. النتائج بالأرقام
جربنا تلات نماذج على نفس بيانات التدريب والاختبار عشان المقارنة تبقى عادلة، ودي كانت نسب الدقة (Accuracy):

- **Logistic Regression:** 96.49%
- **Decision Tree:** 92.98%
- **KNN (من غير Scaling):** 93.86%
- **KNN (بعد الـ Scaling):** 96.49%

---

## 2. أنهي نموذج الأفضل ولييه؟
النموذجين اللي جابوا أعلى نتيجة هما Logistic Regression و KNN بعد توحيد المقياس (بنسبة 96.49% لكل واحد). 
- لو محتاج نموذج سهل يتفهم ونعرف أسباب القرار بتاعه، Logistic Regression بيكون ممتاز.
- لو البيانات بتعتمد على التقارب والمسافات، KNN بيشتغل كويس بس بشرط نعمل Scaling للبيانات الأول.

---

## 3. الفرق قبل وبعد الـ Feature Scaling في KNN
لما جربت KNN من غير ما أعمل StandardScaler، الدقة كانت 93.86%. لكن بعد ما وحدت مقياس الأعمدة النسبة طلعت لـ 96.49%.
السبب ببساطة إن خوارزمية KNN بتحسب مسافات، ولو فيه عمود أرقامه كبيرة وعمود تاني أرقامه صغيرة، الأول هيسيطر على الحسابات ويهمل التاني. الـ Scaling بيخلي كل الأعمدة متساوية في التأثير.
